import { AllDirective } from './all.directive';

describe('AllDirective', () => {
  it('should create an instance', () => {
    const directive = new AllDirective();
    expect(directive).toBeTruthy();
  });
});
